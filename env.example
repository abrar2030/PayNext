#
# PayNext Microservices Environment Variables
# Based on review of backend/pom.xml and service application.properties
# NOTE: Spring Boot applications typically use environment variables to override
# properties in application.properties/yml.
#

# -----------------------------------------------------------------------------
# 1. APPLICATION & SECURITY
# -----------------------------------------------------------------------------
# Secret key for signing JWTs (Implied by jjwt dependency)
JWT_SECRET=
# Spring Boot profile (e.g., dev, prod, test)
SPRING_PROFILES_ACTIVE=prod
# Server port for the specific service (e.g., 8003 for user-service)
SERVER_PORT=

# -----------------------------------------------------------------------------
# 2. DATABASE (H2 for dev, likely PostgreSQL/MySQL for prod)
# -----------------------------------------------------------------------------
# Database connection string for the service
# Example for PostgreSQL: jdbc:postgresql://host:port/dbname
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=
# NOTE: For production, replace the H2 settings in application.properties with these.

# -----------------------------------------------------------------------------
# 3. SERVICE DISCOVERY (Eureka)
# -----------------------------------------------------------------------------
# Eureka Server URL
EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://localhost:8001/eureka/

# -----------------------------------------------------------------------------
# 4. EXTERNAL PAYMENT GATEWAYS
# -----------------------------------------------------------------------------
# Example: Stripe/PayPal/Other Payment Processor API Key
PAYMENT_GATEWAY_API_KEY=
PAYMENT_GATEWAY_SECRET=

# -----------------------------------------------------------------------------
# 5. NOTIFICATIONS (Implied by notification-service)
# -----------------------------------------------------------------------------
# Example: Email Service Credentials
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USERNAME=
EMAIL_PASSWORD=
# Example: SMS Service Credentials (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# -----------------------------------------------------------------------------
# 6. FRAUD DETECTION (Implied by fraud-detection-service)
# -----------------------------------------------------------------------------
# API Key for an external fraud detection service, or internal model path
FRAUD_DETECTION_API_KEY=
ML_MODEL_PATH=

# -----------------------------------------------------------------------------
# 7. CLOUD CONFIG (If using Spring Cloud Config Server)
# -----------------------------------------------------------------------------
# SPRING_CLOUD_CONFIG_URI=http://localhost:8888
